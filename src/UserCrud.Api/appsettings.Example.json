{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "server={localhost};port={port};user={user};password={password};database={name_db}"
  },
  "Jwt": {
    "Key": "enter the secret key",
    "Name": "enter the default username, for example: administrator",
    "Password": "enter the default user password",
    "HoursToExpire": "enter the number of hours the token will be active"
  }
}
